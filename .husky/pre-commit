node scripts/forbidden-pattern-scan.js .

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged -v
else
  echo "  gitleaks not installed — skipping local secret scan"
  echo "  Install v8.30.1: https://github.com/gitleaks/gitleaks/releases/tag/v8.30.1"
fi

pnpm lint-staged
