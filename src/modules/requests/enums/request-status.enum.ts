export enum RequestStatus {
  RECEIVED = 'received',
  PARSING = 'parsing',
  NEEDS_REVIEW = 'needs_review',
  PRICED = 'priced',
  READY = 'ready',
  SENT = 'sent',
  DECLINED = 'declined',
  NEEDS_CLARIFICATION = 'needs_clarification',
  FAILED = 'failed',
}
